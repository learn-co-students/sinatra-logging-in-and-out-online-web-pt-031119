<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in? %>
      <h1>Welcome <%= self.current_user.name %></h1>
      <h2>Your balance:<%= self.current_user.balace %></h2>
      <a href='/logout'>Log Out</a>
    <% else %>
      <% redirect to %>
    <% end %>

  </body>
</html>